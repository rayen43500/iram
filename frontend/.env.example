# Copiez ce fichier en .env puis remplacez par l’IPv4 locale de votre PC (ipconfig).
# Obligatoire si vous utilisez Expo en mode tunnel (--tunnel).
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.X:4000/api
