{
  "name": "iram-banking",
  "version": "1.0.0",
  "private": true,
  "description": "Application bancaire ATB — crédits, simulation, admin",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "backend:verify": "npm run verify --prefix backend",
    "backend:smoke": "npm run smoke --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "verify": "npm run verify --prefix backend && npm run verify --prefix frontend",
    "verify:smoke": "npm run smoke --prefix backend"
  }
}
